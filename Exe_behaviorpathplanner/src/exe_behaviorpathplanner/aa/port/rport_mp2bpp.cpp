///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_mp2bpp.cpp
/// SOFTWARE COMPONENT NAME           : RPort_MP2BPP
/// GENERATED DATE                    : 2025-12-08 15:30:47
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "exe_behaviorpathplanner/aa/port/rport_mp2bpp.h"
 
namespace exe_behaviorpathplanner
{
namespace aa
{
namespace port
{
 
RPort_MP2BPP::RPort_MP2BPP()
    : m_logger(ara::log::CreateLogger("BHPP", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverplanning_missionplanning_missionplanner_debug_goalfootprint{nullptr}
    , m_receiverplanning_missionplanning_missionplanner_debug_processingtimems{nullptr}
    , m_receiverplanning_missionplanning_route{nullptr}
    , m_receiverplanning_missionplanning_routemarker{nullptr}
    , m_receiverplanning_missionplanning_state{nullptr}
    , m_responderplanning_missionplanning_missionplanner_clearroute{nullptr}
    , m_responderplanning_missionplanning_missionplanner_configlogger{nullptr}
    , m_responderplanning_missionplanning_missionplanner_describeparameters{nullptr}
    , m_responderplanning_missionplanning_missionplanner_getparameters{nullptr}
    , m_responderplanning_missionplanning_missionplanner_getparametertypes{nullptr}
    , m_responderplanning_missionplanning_missionplanner_listparameters{nullptr}
    , m_responderplanning_missionplanning_missionplanner_setlaneletroute{nullptr}
    , m_responderplanning_missionplanning_missionplanner_setparameters{nullptr}
    , m_responderplanning_missionplanning_missionplanner_setparametersatomically{nullptr}
    , m_responderplanning_missionplanning_missionplanner_setwaypointroute{nullptr}
{
}
 
RPort_MP2BPP::~RPort_MP2BPP()
{
}
 
void RPort_MP2BPP::Start()
{
    m_logger.LogVerbose() << "RPort_MP2BPP::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"Exe_behaviorpathplanner/AA/RPort_MP2BPP"};
    auto handler = [this](ara::com::ServiceHandleContainer<oss::srv::MP2BPP::proxy::BPP_SI_missionplannerProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = oss::srv::MP2BPP::proxy::BPP_SI_missionplannerProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_MP2BPP::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_MP2BPP::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_MP2BPP::Terminate()
{
    m_logger.LogVerbose() << "RPort_MP2BPP::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        // stop subscribe
        // StopSubscribeplanning_missionplanning_missionplanner_debug_goalfootprint();
        // StopSubscribeplanning_missionplanning_missionplanner_debug_processingtimems();
        StopSubscribeplanning_missionplanning_route();
        // StopSubscribeplanning_missionplanning_routemarker();
        // StopSubscribeplanning_missionplanning_state();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_MP2BPP::Terminate::StopFindService";
    }
}
 
void RPort_MP2BPP::Find(ara::com::ServiceHandleContainer<oss::srv::MP2BPP::proxy::BPP_SI_missionplannerProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_MP2BPP::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_MP2BPP::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_MP2BPP::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<oss::srv::MP2BPP::proxy::BPP_SI_missionplannerProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe events
        // Subscribeplanning_missionplanning_missionplanner_debug_goalfootprint();
        // Subscribeplanning_missionplanning_missionplanner_debug_processingtimems();
        Subscribeplanning_missionplanning_route();
        // Subscribeplanning_missionplanning_routemarker();
        // Subscribeplanning_missionplanning_state();
    }
}
 
void RPort_MP2BPP::Subscribeplanning_missionplanning_missionplanner_debug_goalfootprint()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverplanning_missionplanning_missionplanner_debug_goalfootprint();
        
        // request subscribe
        auto subscribe = m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Subscribeplanning_missionplanning_missionplanner_debug_goalfootprint::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Subscribeplanning_missionplanning_missionplanner_debug_goalfootprint::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::StopSubscribeplanning_missionplanning_missionplanner_debug_goalfootprint()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.Unsubscribe();
        m_logger.LogVerbose() << "RPort_MP2BPP::StopSubscribeplanning_missionplanning_missionplanner_debug_goalfootprint::Unsubscribed";
    }
}
 
void RPort_MP2BPP::RegistReceiverplanning_missionplanning_missionplanner_debug_goalfootprint()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprintTriggered();
        };
        
        // regist callback
        auto callback = m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_missionplanner_debug_goalfootprint::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_missionplanner_debug_goalfootprint::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprintTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.GetNewSamples([&](auto samplePtr) {
                RPort_MP2BPP::ReadDataplanning_missionplanning_missionplanner_debug_goalfootprint(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprint::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprint::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprintCyclic()
{
    while (m_running)
    {
        if (m_found)
        {
            std::lock_guard<std::mutex> lock(m_mutex);
            if (m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_missionplanner_debug_goalfootprint.GetNewSamples([&](auto samplePtr) {
                    RPort_MP2BPP::ReadDataplanning_missionplanning_missionplanner_debug_goalfootprint(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprint::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_goalfootprint::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_MP2BPP::ReadDataplanning_missionplanning_missionplanner_debug_goalfootprint(ara::com::SamplePtr<oss::srv::MP2BPP::proxy::events::planning_missionplanning_missionplanner_debug_goalfootprint::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverplanning_missionplanning_missionplanner_debug_goalfootprint)
    {
        m_receiverplanning_missionplanning_missionplanner_debug_goalfootprint(data);
    }
}
 
void RPort_MP2BPP::RegistEventHandlerplanning_missionplanning_missionplanner_debug_goalfootprint(std::function<void(const oss::srv::MP2BPP::proxy::events::planning_missionplanning_missionplanner_debug_goalfootprint::SampleType&)> handler)
{
    m_receiverplanning_missionplanning_missionplanner_debug_goalfootprint = handler;
}
 
void RPort_MP2BPP::Subscribeplanning_missionplanning_missionplanner_debug_processingtimems()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverplanning_missionplanning_missionplanner_debug_processingtimems();
        
        // request subscribe
        auto subscribe = m_interface->planning_missionplanning_missionplanner_debug_processingtimems.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Subscribeplanning_missionplanning_missionplanner_debug_processingtimems::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Subscribeplanning_missionplanning_missionplanner_debug_processingtimems::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::StopSubscribeplanning_missionplanning_missionplanner_debug_processingtimems()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->planning_missionplanning_missionplanner_debug_processingtimems.Unsubscribe();
        m_logger.LogVerbose() << "RPort_MP2BPP::StopSubscribeplanning_missionplanning_missionplanner_debug_processingtimems::Unsubscribed";
    }
}
 
void RPort_MP2BPP::RegistReceiverplanning_missionplanning_missionplanner_debug_processingtimems()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimemsTriggered();
        };
        
        // regist callback
        auto callback = m_interface->planning_missionplanning_missionplanner_debug_processingtimems.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_missionplanner_debug_processingtimems::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_missionplanner_debug_processingtimems::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimemsTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->planning_missionplanning_missionplanner_debug_processingtimems.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_missionplanner_debug_processingtimems.GetNewSamples([&](auto samplePtr) {
                RPort_MP2BPP::ReadDataplanning_missionplanning_missionplanner_debug_processingtimems(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimems::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimems::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimemsCyclic()
{
    while (m_running)
    {
        if (m_found)
        {
            std::lock_guard<std::mutex> lock(m_mutex);
            if (m_interface->planning_missionplanning_missionplanner_debug_processingtimems.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_missionplanner_debug_processingtimems.GetNewSamples([&](auto samplePtr) {
                    RPort_MP2BPP::ReadDataplanning_missionplanning_missionplanner_debug_processingtimems(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimems::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_missionplanner_debug_processingtimems::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_MP2BPP::ReadDataplanning_missionplanning_missionplanner_debug_processingtimems(ara::com::SamplePtr<oss::srv::MP2BPP::proxy::events::planning_missionplanning_missionplanner_debug_processingtimems::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverplanning_missionplanning_missionplanner_debug_processingtimems)
    {
        m_receiverplanning_missionplanning_missionplanner_debug_processingtimems(data);
    }
}
 
void RPort_MP2BPP::RegistEventHandlerplanning_missionplanning_missionplanner_debug_processingtimems(std::function<void(const oss::srv::MP2BPP::proxy::events::planning_missionplanning_missionplanner_debug_processingtimems::SampleType&)> handler)
{
    m_receiverplanning_missionplanning_missionplanner_debug_processingtimems = handler;
}
 
void RPort_MP2BPP::Subscribeplanning_missionplanning_route()
{
    if (m_found)
    {
        // regist receiver handler for triggered callback
        RegistReceiverplanning_missionplanning_route();
        
        // request subscribe
        auto subscribe = m_interface->planning_missionplanning_route.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Subscribeplanning_missionplanning_route::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Subscribeplanning_missionplanning_route::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::StopSubscribeplanning_missionplanning_route()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->planning_missionplanning_route.Unsubscribe();
        m_logger.LogVerbose() << "RPort_MP2BPP::StopSubscribeplanning_missionplanning_route::Unsubscribed";
    }
}
 
void RPort_MP2BPP::RegistReceiverplanning_missionplanning_route()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventplanning_missionplanning_routeTriggered();
        };
        
        // regist callback
        auto callback = m_interface->planning_missionplanning_route.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_route::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_route::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_routeTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->planning_missionplanning_route.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_route.GetNewSamples([&](auto samplePtr) {
                RPort_MP2BPP::ReadDataplanning_missionplanning_route(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_route::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_route::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_routeCyclic()
{
    while (m_running)
    {
        if (m_found)
        {
            std::lock_guard<std::mutex> lock(m_mutex);
            if (m_interface->planning_missionplanning_route.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_route.GetNewSamples([&](auto samplePtr) {
                    RPort_MP2BPP::ReadDataplanning_missionplanning_route(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_route::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_route::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_MP2BPP::ReadDataplanning_missionplanning_route(ara::com::SamplePtr<oss::srv::MP2BPP::proxy::events::planning_missionplanning_route::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverplanning_missionplanning_route)
    {
        m_receiverplanning_missionplanning_route(data);
    }
}
 
void RPort_MP2BPP::RegistEventHandlerplanning_missionplanning_route(std::function<void(const oss::srv::MP2BPP::proxy::events::planning_missionplanning_route::SampleType&)> handler)
{
    m_receiverplanning_missionplanning_route = handler;
}
 
void RPort_MP2BPP::Subscribeplanning_missionplanning_routemarker()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverplanning_missionplanning_routemarker();
        
        // request subscribe
        auto subscribe = m_interface->planning_missionplanning_routemarker.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Subscribeplanning_missionplanning_routemarker::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Subscribeplanning_missionplanning_routemarker::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::StopSubscribeplanning_missionplanning_routemarker()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->planning_missionplanning_routemarker.Unsubscribe();
        m_logger.LogVerbose() << "RPort_MP2BPP::StopSubscribeplanning_missionplanning_routemarker::Unsubscribed";
    }
}
 
void RPort_MP2BPP::RegistReceiverplanning_missionplanning_routemarker()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventplanning_missionplanning_routemarkerTriggered();
        };
        
        // regist callback
        auto callback = m_interface->planning_missionplanning_routemarker.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_routemarker::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_routemarker::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_routemarkerTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->planning_missionplanning_routemarker.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_routemarker.GetNewSamples([&](auto samplePtr) {
                RPort_MP2BPP::ReadDataplanning_missionplanning_routemarker(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_routemarker::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_routemarker::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_routemarkerCyclic()
{
    while (m_running)
    {
        if (m_found)
        {
            std::lock_guard<std::mutex> lock(m_mutex);
            if (m_interface->planning_missionplanning_routemarker.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_routemarker.GetNewSamples([&](auto samplePtr) {
                    RPort_MP2BPP::ReadDataplanning_missionplanning_routemarker(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_routemarker::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_routemarker::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_MP2BPP::ReadDataplanning_missionplanning_routemarker(ara::com::SamplePtr<oss::srv::MP2BPP::proxy::events::planning_missionplanning_routemarker::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverplanning_missionplanning_routemarker)
    {
        m_receiverplanning_missionplanning_routemarker(data);
    }
}
 
void RPort_MP2BPP::RegistEventHandlerplanning_missionplanning_routemarker(std::function<void(const oss::srv::MP2BPP::proxy::events::planning_missionplanning_routemarker::SampleType&)> handler)
{
    m_receiverplanning_missionplanning_routemarker = handler;
}
 
void RPort_MP2BPP::Subscribeplanning_missionplanning_state()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverplanning_missionplanning_state();
        
        // request subscribe
        auto subscribe = m_interface->planning_missionplanning_state.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Subscribeplanning_missionplanning_state::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Subscribeplanning_missionplanning_state::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::StopSubscribeplanning_missionplanning_state()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->planning_missionplanning_state.Unsubscribe();
        m_logger.LogVerbose() << "RPort_MP2BPP::StopSubscribeplanning_missionplanning_state::Unsubscribed";
    }
}
 
void RPort_MP2BPP::RegistReceiverplanning_missionplanning_state()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventplanning_missionplanning_stateTriggered();
        };
        
        // regist callback
        auto callback = m_interface->planning_missionplanning_state.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_state::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::RegistReceiverplanning_missionplanning_state::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_stateTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->planning_missionplanning_state.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_state.GetNewSamples([&](auto samplePtr) {
                RPort_MP2BPP::ReadDataplanning_missionplanning_state(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_state::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_state::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_MP2BPP::ReceiveEventplanning_missionplanning_stateCyclic()
{
    while (m_running)
    {
        if (m_found)
        {
            std::lock_guard<std::mutex> lock(m_mutex);
            if (m_interface->planning_missionplanning_state.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->planning_missionplanning_state.GetNewSamples([&](auto samplePtr) {
                    RPort_MP2BPP::ReadDataplanning_missionplanning_state(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_state::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_MP2BPP::ReceiveEventplanning_missionplanning_state::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_MP2BPP::ReadDataplanning_missionplanning_state(ara::com::SamplePtr<oss::srv::MP2BPP::proxy::events::planning_missionplanning_state::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverplanning_missionplanning_state)
    {
        m_receiverplanning_missionplanning_state(data);
    }
}
 
void RPort_MP2BPP::RegistEventHandlerplanning_missionplanning_state(std::function<void(const oss::srv::MP2BPP::proxy::events::planning_missionplanning_state::SampleType&)> handler)
{
    m_receiverplanning_missionplanning_state = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_clearroute()
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_clearroute();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_clearroute::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_clearroute)
            {
                m_responderplanning_missionplanning_missionplanner_clearroute(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_clearroute::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_clearroute(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_clearroute::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_clearroute = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_configlogger(const oss::type::logging_demo::srv::ConfigLogger_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_configlogger(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_configlogger::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_configlogger)
            {
                m_responderplanning_missionplanning_missionplanner_configlogger(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_configlogger::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_configlogger(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_configlogger::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_configlogger = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_describeparameters(const oss::type::rcl_interfaces::srv::DescribeParameters_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_describeparameters(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_describeparameters::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_describeparameters)
            {
                m_responderplanning_missionplanning_missionplanner_describeparameters(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_describeparameters::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_describeparameters(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_describeparameters::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_describeparameters = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_getparameters(const oss::type::rcl_interfaces::srv::GetParameters_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_getparameters(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_getparameters::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_getparameters)
            {
                m_responderplanning_missionplanning_missionplanner_getparameters(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_getparameters::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_getparameters(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_getparameters::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_getparameters = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_getparametertypes(const oss::type::rcl_interfaces::srv::GetParameterTypes_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_getparametertypes(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_getparametertypes::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_getparametertypes)
            {
                m_responderplanning_missionplanning_missionplanner_getparametertypes(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_getparametertypes::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_getparametertypes(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_getparametertypes::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_getparametertypes = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_listparameters(const oss::type::rcl_interfaces::srv::ListParameters_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_listparameters(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_listparameters::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_listparameters)
            {
                m_responderplanning_missionplanning_missionplanner_listparameters(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_listparameters::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_listparameters(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_listparameters::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_listparameters = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setlaneletroute(const oss::type::tier4_planning_msgs::srv::SetLaneletRoute_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_setlaneletroute(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setlaneletroute::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_setlaneletroute)
            {
                m_responderplanning_missionplanning_missionplanner_setlaneletroute(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setlaneletroute::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_setlaneletroute(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_setlaneletroute::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_setlaneletroute = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setparameters(const oss::type::rcl_interfaces::srv::SetParameters_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_setparameters(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setparameters::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_setparameters)
            {
                m_responderplanning_missionplanning_missionplanner_setparameters(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setparameters::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_setparameters(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_setparameters::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_setparameters = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setparametersatomically(const oss::type::rcl_interfaces::srv::SetParametersAtomically_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_setparametersatomically(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setparametersatomically::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_setparametersatomically)
            {
                m_responderplanning_missionplanning_missionplanner_setparametersatomically(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setparametersatomically::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_setparametersatomically(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_setparametersatomically::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_setparametersatomically = handler;
}
 
void RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setwaypointroute(const oss::type::tier4_planning_msgs::srv::SetWaypointRoute_Request& req)
{
    if (m_found)
    {
        auto request = m_interface->planning_missionplanning_missionplanner_setwaypointroute(req);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setwaypointroute::Responded";
            
            auto result = response.Value();
            if (m_responderplanning_missionplanning_missionplanner_setwaypointroute)
            {
                m_responderplanning_missionplanning_missionplanner_setwaypointroute(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_MP2BPP::Requestplanning_missionplanning_missionplanner_setwaypointroute::" << response.Error().Message();
        }
    }
}
 
void RPort_MP2BPP::RegistResponseHandlerplanning_missionplanning_missionplanner_setwaypointroute(std::function<void(const oss::srv::MP2BPP::proxy::methods::planning_missionplanning_missionplanner_setwaypointroute::Output&)> handler)
{
    m_responderplanning_missionplanning_missionplanner_setwaypointroute = handler;
}
 
} /// namespace port
} /// namespace aa
} /// namespace exe_behaviorpathplanner
 
/// EOF